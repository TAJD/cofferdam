# SQLite database (legacy br backend; kept until cleanup)
*.db
*.db-journal
*.db-shm
*.db-wal

# bd Dolt embedded backend (per-machine working DB)
embeddeddolt/
dolt/
backup/
dolt-server.pid
dolt-server.port
dolt-server.log
.bd-dolt-ok

# Lock files
*.lock

# Temporary
last-touched
*.tmp

# Legacy br state (remove once br cleanup complete)
.br_history/
.local_version
config.yaml
metadata.json

# Sync / export state (local-only, per-machine)
.sync.lock
sync_base.jsonl
export-state.json
interactions.jsonl

# Merge artifacts (temporary files from 3-way merge)
beads.base.jsonl
beads.base.meta.json
beads.left.jsonl
beads.left.meta.json
beads.right.jsonl
beads.right.meta.json

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock
sync-state.json

# Worktree redirect file
redirect

# bv (beads viewer) lock file
.bv.lock
