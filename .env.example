# Local secrets — copy to .env and fill in. .env is gitignored.

# npm publish token. Get one from https://www.npmjs.com/settings/<you>/tokens
# Used by scripts/release.ps1 if you want to publish to npm from a local cut
# (the GitHub Actions release.yml uses the NPM_TOKEN secret on the repo itself,
# not this file).
NPM_TOKEN=
