// Internal definitions, re-exported through `src/index.ts` (allow-listed)
// and through `src/leaf.ts` (NOT allow-listed). Used by the fixture to
// pin (a) public-api re-exports stay silent and (b) non-public-api
// re-exports of the same symbol still fire.
export const ALPHA = 1;
export const BETA = 2;
