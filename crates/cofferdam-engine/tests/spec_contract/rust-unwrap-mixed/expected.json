{
  "findings": [
    {
      "id": "Rust.NoUnwrapInLib",
      "file": "src/lib_code.rs",
      "line": 8,
      "column": 22,
      "message": "`.unwrap()` in library code panics with no diagnostic context; return `Result` and propagate via `?` or use `.expect(\"<reason>\")` when the value is provably infallible."
    }
  ]
}
