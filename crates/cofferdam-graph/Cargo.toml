[package]
name = "cofferdam-graph"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Canonical graph schema for cross-file rules (cd-9hp.9). Type-only at checkpoint 1; petgraph backend lands in checkpoint 2."

[dependencies]
# cd-9hp.9 cp2 — petgraph backend lives behind cofferdam-graph::store
# so cofferdam-core stays dependency-light per workspace guidance.
# Only this crate (and downstream consumers that explicitly opt in)
# sees petgraph.
cofferdam-core = { workspace = true }
petgraph = "0.8"
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
smol_str = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
