[package]
name = "cofferdam-html"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "HTML source-code adapter for cofferdam — tree-sitter-html + checks specific to HTML documents (CD-83)"

[lints]
workspace = true

[dependencies]
cofferdam-core = { workspace = true }
# tree-sitter pin: bump together with tree-sitter-html to keep ABI in sync.
# Mirrors cofferdam-rust's tree-sitter = "0.25" pin.
tree-sitter = "0.26"
tree-sitter-html = "0.23"
thiserror = { workspace = true }

[dev-dependencies]
