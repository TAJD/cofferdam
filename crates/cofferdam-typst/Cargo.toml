[package]
name = "cofferdam-typst"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Typst package linter — standalone checks for Typst Universe submission hygiene (typst.toml manifest, license, README, bundle)."

[lints]
workspace = true

[dependencies]
cofferdam-core = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }
# Not workspace-pinned elsewhere; this crate needs key/value byte spans
# for typst.toml so findings point at the offending line, not line 1.
toml_edit = "0.22"
