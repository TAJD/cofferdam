node_modules/
.vitepress/cache/
.vitepress/dist/
# Regenerated by scripts/build-llms-full.mjs on every docs:build (CD-63 E2) --
# unlike public/llms.txt / public/checks.json, nothing gates its freshness,
# so it isn't committed to avoid silent staleness between builds.
public/llms-full.txt
