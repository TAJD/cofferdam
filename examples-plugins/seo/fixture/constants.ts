// Shared constants imported by page fixtures — exercises
// SeoNonEmptyDescription's cross-file `resolveLiteral` resolution.
// Does not itself match `page.ts`/`page.tsx` so no page-level checks
// fire against it directly.

export const goodDescription = "A thorough guide to widget maintenance.";
export const badDescription = "";
